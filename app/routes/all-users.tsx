import { Header } from "../../components";

const AllUsers = () => {
	return (
		<main className={"dashboard wrapper"}>
			<Header title={"Trips Page"} description={"Check out our current user in real time"} />
			<p>All Users</p>
		</main>
	);
};

export default AllUsers;
